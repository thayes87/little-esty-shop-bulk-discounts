<%= render partial: 'nav_bar' %>

<%= form_with model: [:admin, @merchant], local: true, id: "update_merchant" do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>
